export type ReportShape = {
    date: string,
    product: string,
    quantity: number
}

export type StockShape = {
    title: string,
    category: string,
    quantity: number,
    min_quantity: number
}